function AlertButton(message, childeren) {
    return (
        <button onClick={() => alert(message)}>{childeren}</button>
    )
}

export default function Event02() {

    return (
        <div>

        </div>
    )
}